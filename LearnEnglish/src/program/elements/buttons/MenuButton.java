package program.elements.buttons;

import program.settings.MyColor;

public class MenuButton extends Button{
    public MenuButton(String name) {
        super(name);
        setBackground(MyColor.ORANGE_LIGHT);
    }
}
