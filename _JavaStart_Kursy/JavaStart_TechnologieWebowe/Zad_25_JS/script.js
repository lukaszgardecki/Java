const day = 123;
const hours = day * 24;
const mins = hours * 60;
const secs = mins * 60;

const text = "Od początku roku minęło " + day + " dni. To " + hours + " godzin, " + mins + " minut lub " + secs + " sekund.";

alert(text)
console.log(text)